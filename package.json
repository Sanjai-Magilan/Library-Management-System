{
  "name": "library-management-system",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "docker:start": "docker run -d --name library-backend --network library-net --env-file .env -p 5000:5000 library-backend",
    "docker:stop": "docker stop library-backend && docker rm library-backend"
    },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^17.2.2",
    "express": "^5.1.0",
    "mongoose": "^8.18.0",
    "moongoose": "^0.0.1"
  }
}
